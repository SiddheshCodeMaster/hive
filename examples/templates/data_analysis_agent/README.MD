This PR introduces a simple Data Analysis Agent example demonstrating how Hive can be used to analyze structured datasets and generate summary insights.

Workflow:
intake -> analysis -> report

The agent accepts a dataset path, computes basic statistics, and generates a summary report.

This example is intended as a lightweight template for developers exploring data-driven agents within the Hive framework.
